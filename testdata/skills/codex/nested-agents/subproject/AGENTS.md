# Subproject Guidelines

This AGENTS.md file is nested in a subdirectory and should be discovered recursively.

## Subproject-Specific Rules

- This subproject uses different conventions than the parent project
- Always check the subproject README before making changes
