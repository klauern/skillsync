// Package parser provides platform-specific skill parsers.
// Each platform (Claude Code, Cursor, Codex) has its own parser
// implementation that knows how to discover and parse skills
// from that platform's configuration format.
package parser
